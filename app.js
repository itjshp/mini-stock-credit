
const DB_NAME="khaikhong-v2-db",DB_VERSION=1,STORES=["products","customers","bills","bill_items","payments","stock_movements","settings"];let db,state={products:[],customers:[],bills:[],bill_items:[],payments:[],stock_movements:[],settings:[]},cart=[],selectedLedgerCustomerId="",currentNumberInput=null,numberPadValue="",deferredPrompt=null;const $=id=>document.getElementById(id),money=n=>Number(n||0).toLocaleString("th-TH",{minimumFractionDigits:2,maximumFractionDigits:2}),today=()=>new Date().toISOString().slice(0,10),uid=()=>crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`;
function showToast(msg){const el=$("toast");el.textContent=msg;el.classList.remove("hidden");setTimeout(()=>el.classList.add("hidden"),2400)}
function openDB(){return new Promise((res,rej)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=()=>{const d=r.result;STORES.forEach(s=>{if(!d.objectStoreNames.contains(s))d.createObjectStore(s,{keyPath:"id"})})};r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}
function store(n,m="readonly"){return db.transaction(n,m).objectStore(n)}function getAll(n){return new Promise((res,rej)=>{const r=store(n).getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}function put(n,item){return new Promise((res,rej)=>{const r=store(n,"readwrite").put(item);r.onsuccess=()=>res(item);r.onerror=()=>rej(r.error)})}function del(n,id){return new Promise((res,rej)=>{const r=store(n,"readwrite").delete(id);r.onsuccess=()=>res(true);r.onerror=()=>rej(r.error)})}function clearStore(n){return new Promise((res,rej)=>{const r=store(n,"readwrite").clear();r.onsuccess=()=>res(true);r.onerror=()=>rej(r.error)})}
async function loadState(){const rows=await Promise.all(STORES.map(getAll));STORES.forEach((s,i)=>state[s]=rows[i]);state.products.sort((a,b)=>(a.name||"").localeCompare(b.name||"","th"));state.customers.sort((a,b)=>(a.name||"").localeCompare(b.name||"","th"));state.bills.sort((a,b)=>`${b.date||""} ${b.createdAt||""}`.localeCompare(`${a.date||""} ${a.createdAt||""}`));state.stock_movements.sort((a,b)=>`${b.date||""} ${b.createdAt||""}`.localeCompare(`${a.date||""} ${a.createdAt||""}`));state.payments.sort((a,b)=>`${b.date||""} ${b.createdAt||""}`.localeCompare(`${a.date||""} ${a.createdAt||""}`));renderAll()}
function customerName(id){return id?(state.customers.find(c=>c.id===id)?.name||"-"):"ลูกค้าเงินสด"}function productById(id){return state.products.find(p=>p.id===id)}function billItems(id){return state.bill_items.filter(i=>i.billId===id)}function activeBills(){return state.bills.filter(b=>b.status!=="cancelled")}function customerDebt(id){const credit=activeBills().filter(b=>b.customerId===id&&(b.paymentType==="credit"||b.creditAmount>0)).reduce((s,b)=>s+Number(b.creditAmount||0),0),paid=state.payments.filter(p=>p.customerId===id).reduce((s,p)=>s+Number(p.amount||0),0);return Math.max(0,credit-paid)}function totalDebt(){return state.customers.reduce((s,c)=>s+customerDebt(c.id),0)}function billBadge(b){if(b.status==="cancelled")return`<span class="badge badge-cancelled">ยกเลิก</span>`;if(b.paymentType==="credit"||Number(b.creditAmount||0)>0)return`<span class="badge badge-credit">เครดิต</span>`;return`<span class="badge badge-paid">ชำระแล้ว</span>`}function nextBillNo(){const s=state.settings.find(x=>x.id==="main")||{};return`KH-${String(Number(s.nextBillNo||1)).padStart(6,"0")}`}async function incBillNo(){const s=state.settings.find(x=>x.id==="main")||{id:"main",nextBillNo:1};s.nextBillNo=Number(s.nextBillNo||1)+1;s.updatedAt=new Date().toISOString();await put("settings",s)}
async function recomputeInventory(){const products=await getAll("products"),mov=(await getAll("stock_movements")).sort((a,b)=>`${a.date||""} ${a.createdAt||""}`.localeCompare(`${b.date||""} ${b.createdAt||""}`)),map=new Map(products.map(p=>[p.id,{...p,stockQty:0,avgCost:0}]));for(const m of mov){const p=map.get(m.productId);if(!p)continue;const inn=Number(m.qtyIn||0),out=Number(m.qtyOut||0),cost=Number(m.unitCost||0);if(inn>0){const old=Number(p.stockQty||0),val=old*Number(p.avgCost||0),nq=old+inn;p.avgCost=nq>0?(val+inn*cost)/nq:0;p.stockQty=nq}if(out>0)p.stockQty=Number(p.stockQty||0)-out}for(const p of map.values())await put("products",{...p,updatedAt:new Date().toISOString()})}
async function recalcBills(){const bills=await getAll("bills"),items=await getAll("bill_items");for(const b of bills){const its=items.filter(i=>i.billId===b.id);b.subtotal=its.reduce((s,i)=>s+Number(i.revenue||0),0);b.costTotal=its.reduce((s,i)=>s+Number(i.cost||0),0);b.profitTotal=its.reduce((s,i)=>s+Number(i.profit||0),0);if(b.status!=="cancelled"){b.creditAmount=b.paymentType==="credit"?Math.max(0,Number(b.subtotal||0)-Number(b.paidAmount||0)):0;b.status=b.creditAmount>0?(Number(b.paidAmount||0)>0?"partial":"credit"):"paid"}await put("bills",b)}}
function setOptions(id,rows,placeholder,fn){const el=$(id);if(!el)return;const cur=el.value;el.innerHTML=`<option value="">${placeholder}</option>`+rows.map(r=>`<option value="${r.id}">${fn(r)}</option>`).join("");if([...el.options].some(o=>o.value===cur))el.value=cur}
function renderAll(){renderSelects();renderSale();renderProducts();renderCustomers();renderSummary();renderMovements();renderLedger();renderPayments();renderReports();renderBackupStatus()}function renderSelects(){setOptions("billCustomer",state.customers,"เลือกลูกค้า",c=>`${c.name} • ค้าง ${money(customerDebt(c.id))}`);setOptions("purchaseProduct",state.products,"เลือกสินค้า",p=>`${p.name} • เหลือ ${money(p.stockQty)} ${p.unit||""}`);setOptions("paymentCustomer",state.customers,"เลือกลูกค้า",c=>`${c.name} • ค้าง ${money(customerDebt(c.id))}`);setOptions("reportCustomer",state.customers,"ลูกค้าทั้งหมด",c=>c.name)}
function renderSale(){const q=($("saleSearch")?.value||"").toLowerCase().trim(),products=state.products.filter(p=>!q||`${p.name} ${p.unit||""} ${p.note||""}`.toLowerCase().includes(q)).slice(0,24);$("quickProducts").innerHTML=products.map(p=>`<button class="product-tile" onclick="addProductToCart('${p.id}')" type="button"><strong>${p.name}</strong><small>เหลือ ${money(p.stockQty)} ${p.unit||""} • ทุน ${money(p.avgCost)}</small><div class="tile-price"><span>ขาย ${money(p.price)}</span><span>${Number(p.stockQty||0)<=Number(p.minStock||0)&&Number(p.minStock||0)>0?"ใกล้หมด":"พร้อมขาย"}</span></div></button>`).join("")||`<div class="list-item"><div><strong>ไม่พบสินค้า</strong><small>เพิ่มสินค้าได้ที่เมนูสินค้า</small></div></div>`;$("cartItems").innerHTML=cart.map(i=>`<div class="cart-item"><div class="cart-item-top"><strong>${i.name}</strong><button class="small-btn small-danger" onclick="removeCartItem('${i.productId}')">ลบ</button></div><div class="cart-controls"><button class="qty-btn" onclick="changeCartQty('${i.productId}',-1)">−</button><span class="money">${money(i.qty)}</span><button class="qty-btn" onclick="changeCartQty('${i.productId}',1)">+</button><span class="cart-price">${money(i.qty*i.unitPrice)}</span></div></div>`).join("")||`<div class="list-item"><div><strong>ยังไม่มีสินค้าในบิล</strong><small>แตะสินค้าเพื่อเพิ่มลงบิล</small></div></div>`;const total=cart.reduce((s,i)=>s+i.qty*i.unitPrice,0),cost=cart.reduce((s,i)=>s+i.qty*i.unitCost,0);$("cartCount").textContent=cart.length;$("cartTotal").textContent=money(total);$("cartProfit").textContent=money(total-cost);$("currentBillMeta").textContent=`เลขบิลถัดไป: ${nextBillNo()}`}
window.addProductToCart=id=>{const p=productById(id);if(!p)return;const ex=cart.find(i=>i.productId===id);if(ex)ex.qty+=1;else cart.push({productId:id,name:p.name,unit:p.unit||"",qty:1,unitPrice:Number(p.price||0),unitCost:Number(p.avgCost||0)});renderSale();showToast(`เพิ่ม ${p.name} ลงบิล`)};window.changeCartQty=(id,d)=>{const i=cart.find(x=>x.productId===id);if(!i)return;i.qty=Math.max(0.01,Number(i.qty||0)+d);renderSale()};window.removeCartItem=id=>{cart=cart.filter(i=>i.productId!==id);renderSale()};function clearCart(){cart=[];$("paidAmount").value=0;$("billNote").value="";renderSale()}
async function saveBill(){if(cart.length===0)return alert("ยังไม่มีสินค้าในบิล");const type=$("paymentType").value,cid=$("billCustomer").value;if(type==="credit"&&!cid)return alert("ขายเครดิตต้องเลือกลูกค้า");for(const item of cart){const p=productById(item.productId);if(!p||Number(item.qty)>Number(p.stockQty||0))return alert(`สต็อกไม่พอ: ${item.name}`)}const billId=uid(),billNo=nextBillNo(),date=$("billDate").value||today(),subtotal=cart.reduce((s,i)=>s+i.qty*i.unitPrice,0),costTotal=cart.reduce((s,i)=>s+i.qty*i.unitCost,0);let paid=Number($("paidAmount").value||0);if(type==="cash")paid=subtotal;if(paid>subtotal)return alert("รับเงินมากกว่ายอดบิลไม่ได้");const credit=type==="credit"?Math.max(0,subtotal-paid):0,bill={id:billId,billNo,date,customerId:cid||"",paymentType:type,subtotal,costTotal,profitTotal:subtotal-costTotal,paidAmount:paid,creditAmount:credit,status:credit>0?(paid>0?"partial":"credit"):"paid",note:$("billNote").value.trim(),createdAt:new Date().toISOString()};await put("bills",bill);for(const item of cart){await put("bill_items",{id:uid(),billId,productId:item.productId,productNameSnapshot:item.name,qty:item.qty,unitPrice:item.unitPrice,unitCost:item.unitCost,revenue:item.qty*item.unitPrice,cost:item.qty*item.unitCost,profit:item.qty*(item.unitPrice-item.unitCost)});await put("stock_movements",{id:uid(),productId:item.productId,type:"sale",refType:"bill",refId:billId,date,qtyIn:0,qtyOut:item.qty,unitCost:item.unitCost,note:`ขายบิล ${billNo}`,createdAt:new Date().toISOString()})}await incBillNo();await recomputeInventory();await loadState();clearCart();showToast(`บันทึกขาย ${billNo} แล้ว`)}
async function cancelBill(id){const b=state.bills.find(x=>x.id===id);if(!b||b.status==="cancelled")return;if(!confirm(`ยกเลิกบิล ${b.billNo}?\\nระบบจะคืนสต็อกให้อัตโนมัติ`))return;for(const item of billItems(id)){await put("stock_movements",{id:uid(),productId:item.productId,type:"sale_cancel",refType:"bill",refId:id,date:today(),qtyIn:item.qty,qtyOut:0,unitCost:item.unitCost,note:`ยกเลิกบิล ${b.billNo}`,createdAt:new Date().toISOString()})}b.status="cancelled";b.creditAmount=0;b.updatedAt=new Date().toISOString();await put("bills",b);await recomputeInventory();await loadState();showToast(`ยกเลิกบิล ${b.billNo} แล้ว`)}window.cancelBill=cancelBill;
function renderProducts(){const q=($("productSearch")?.value||"").toLowerCase().trim(),rows=state.products.filter(p=>!q||`${p.name} ${p.unit||""} ${p.note||""}`.toLowerCase().includes(q));$("productsTable").innerHTML=rows.map(p=>`<tr><td><strong>${p.name}</strong><br><small>${p.unit||""} ${p.note?`• ${p.note}`:""}</small></td><td>${money(p.stockQty)} ${p.unit||""}</td><td>${money(p.avgCost)}</td><td>${money(p.price)}</td><td>${Number(p.minStock||0)>0&&Number(p.stockQty||0)<=Number(p.minStock||0)?'<span class="low">ใกล้หมด</span>':'<span class="ok-stock">ปกติ</span>'}</td><td><div class="row-actions"><button class="small-btn small-edit" onclick="editProduct('${p.id}')">แก้ไข</button><button class="small-btn small-danger" onclick="deleteProduct('${p.id}')">ลบ</button></div></td></tr>`).join("")||`<tr><td colspan="6">ยังไม่มีสินค้า</td></tr>`}
function resetProductForm(){["productId","productName","productUnit","productNote"].forEach(id=>$(id).value="");$("productPrice").value=0;$("productMin").value=0;$("productSubmitBtn").textContent="บันทึกสินค้า"}window.editProduct=id=>{const p=productById(id);if(!p)return;$("productId").value=p.id;$("productName").value=p.name||"";$("productUnit").value=p.unit||"";$("productPrice").value=p.price||0;$("productMin").value=p.minStock||0;$("productNote").value=p.note||"";$("productSubmitBtn").textContent="อัปเดตสินค้า";switchTab("products")};async function deleteProduct(id){if(state.bill_items.some(i=>i.productId===id)||state.stock_movements.some(m=>m.productId===id))return alert("สินค้านี้มีประวัติแล้ว ไม่ควรลบ ให้แก้ชื่อแทน");if(confirm("ลบสินค้า?")){await del("products",id);await loadState()}}window.deleteProduct=deleteProduct;
function renderCustomers(){const q=($("customerSearch")?.value||"").toLowerCase().trim(),rows=state.customers.filter(c=>!q||`${c.name} ${c.phone||""} ${c.type||""}`.toLowerCase().includes(q));$("customersTable").innerHTML=rows.map(c=>`<tr><td><strong>${c.name}</strong><br><small>${c.phone||""} ${c.note?`• ${c.note}`:""}</small></td><td>${c.type||"-"}</td><td class="${customerDebt(c.id)>0?"negative":""}">${money(customerDebt(c.id))}</td><td>${money(c.creditLimit||0)}</td><td><div class="row-actions"><button class="small-btn" onclick="openLedger('${c.id}')">สมุดบัญชี</button><button class="small-btn small-edit" onclick="editCustomer('${c.id}')">แก้ไข</button><button class="small-btn small-danger" onclick="deleteCustomer('${c.id}')">ลบ</button></div></td></tr>`).join("")||`<tr><td colspan="5">ยังไม่มีลูกค้า</td></tr>`}
function resetCustomerForm(){["customerId","customerName","customerPhone","customerNote"].forEach(id=>$(id).value="");$("customerType").value="ทั่วไป";$("customerLimit").value=0;$("customerDays").value=0;$("customerSubmitBtn").textContent="บันทึกลูกค้า"}window.editCustomer=id=>{const c=state.customers.find(x=>x.id===id);if(!c)return;$("customerId").value=c.id;$("customerName").value=c.name||"";$("customerType").value=c.type||"ทั่วไป";$("customerPhone").value=c.phone||"";$("customerLimit").value=c.creditLimit||0;$("customerDays").value=c.creditDays||0;$("customerNote").value=c.note||"";$("customerSubmitBtn").textContent="อัปเดตลูกค้า";switchTab("customers")};async function deleteCustomer(id){if(state.bills.some(b=>b.customerId===id)||state.payments.some(p=>p.customerId===id))return alert("ลูกค้านี้มีประวัติแล้ว ไม่ควรลบ ให้แก้ชื่อแทน");if(confirm("ลบลูกค้า?")){await del("customers",id);await loadState()}}window.deleteCustomer=deleteCustomer;
function billItemsCount(id){return billItems(id).length}function billRow(b){return `<div class="list-item"><div><strong>${b.billNo} ${billBadge(b)}</strong><small>${b.date} • ${customerName(b.customerId)} • ${billItemsCount(b.id)} รายการ</small></div><div><div class="money">${money(b.subtotal)}</div><small class="${b.profitTotal>=0?"positive":"negative"}">กำไร ${money(b.profitTotal)}</small></div></div>`}
function renderSummary(){const active=activeBills(),todayBills=active.filter(b=>b.date===today()),monthBills=active.filter(b=>String(b.date||"").startsWith(today().slice(0,7)));$("todaySales").textContent=money(todayBills.reduce((s,b)=>s+Number(b.subtotal||0),0));$("todayProfit").textContent=money(todayBills.reduce((s,b)=>s+Number(b.profitTotal||0),0));$("monthSales").textContent=money(monthBills.reduce((s,b)=>s+Number(b.subtotal||0),0));$("totalCredit").textContent=money(totalDebt());$("lowStockList").innerHTML=state.products.filter(p=>Number(p.minStock||0)>0&&Number(p.stockQty||0)<=Number(p.minStock||0)).map(p=>`<div class="list-item"><div><strong>${p.name}</strong><small>เหลือ ${money(p.stockQty)} ${p.unit||""} / ขั้นต่ำ ${money(p.minStock)}</small></div><div class="low">ใกล้หมด</div></div>`).join("")||`<div class="list-item"><div><strong>ไม่มีสินค้าใกล้หมด</strong></div></div>`;const debtors=state.customers.map(c=>({...c,debt:customerDebt(c.id)})).filter(c=>c.debt>0).sort((a,b)=>b.debt-a.debt).slice(0,5);$("topDebtors").innerHTML=debtors.map(c=>`<div class="list-item"><div><strong>${c.name}</strong><small>${c.phone||""}</small></div><div class="money negative">${money(c.debt)}</div></div>`).join("")||`<div class="list-item"><div><strong>ยังไม่มีลูกหนี้</strong></div></div>`;$("summaryRecentBills").innerHTML=active.slice(0,5).map(b=>billRow(b)).join("")||`<div class="list-item"><div><strong>ยังไม่มีบิลขาย</strong></div></div>`;renderBackupStatus()}function renderRecentBills(){$("recentBills").innerHTML=state.bills.slice(0,8).map(b=>billRow(b)).join("")||`<div class="list-item"><div><strong>ยังไม่มีบิลขาย</strong></div></div>`}
function renderMovements(){$("movementList").innerHTML=state.stock_movements.slice(0,25).map(m=>`<div class="list-item"><div><strong>${productById(m.productId)?.name||"-"}</strong><small>${m.date} • ${m.type} • ${m.note||""}</small></div><div class="money">${m.qtyIn?`+${money(m.qtyIn)}`:`-${money(m.qtyOut)}`}</div></div>`).join("")||`<div class="list-item"><div><strong>ยังไม่มีประวัติสต็อก</strong></div></div>`}
function renderLedger(){const q=($("ledgerSearch")?.value||"").toLowerCase().trim(),rows=state.customers.map(c=>({...c,debt:customerDebt(c.id)})).filter(c=>!q||`${c.name} ${c.phone||""}`.toLowerCase().includes(q)).sort((a,b)=>b.debt-a.debt);$("ledgerCustomers").innerHTML=rows.map(c=>`<div class="list-item" onclick="openLedger('${c.id}')"><div><strong>${c.name}</strong><small>${c.type||""}</small></div><div class="money ${c.debt>0?"negative":"positive"}">${money(c.debt)}</div></div>`).join("")||`<div class="list-item"><div><strong>ยังไม่มีลูกค้า</strong></div></div>`;const c=state.customers.find(x=>x.id===selectedLedgerCustomerId);if(!c){$("ledgerTitle").textContent="สมุดบัญชี";$("ledgerBalance").textContent=money(0);$("ledgerCreditSales").textContent=money(0);$("ledgerPaid").textContent=money(0);$("ledgerEntries").innerHTML=`<div class="list-item"><div><strong>เลือกลูกค้าด้านซ้าย</strong></div></div>`;return}const bills=activeBills().filter(b=>b.customerId===c.id&&b.paymentType==="credit"),pays=state.payments.filter(p=>p.customerId===c.id);$("ledgerTitle").textContent=c.name;$("ledgerBalance").textContent=money(customerDebt(c.id));$("ledgerCreditSales").textContent=money(bills.reduce((s,b)=>s+Number(b.subtotal||0),0));$("ledgerPaid").textContent=money(pays.reduce((s,p)=>s+Number(p.amount||0),0));const entries=[...bills.map(b=>({date:b.date,title:`บิล ${b.billNo}`,detail:`ขายเครดิต ${billItemsCount(b.id)} รายการ`,amount:Number(b.creditAmount||0)})),...pays.map(p=>({date:p.date,title:"รับเงิน",detail:p.method||"",amount:-Number(p.amount||0)}))].sort((a,b)=>String(b.date).localeCompare(String(a.date)));$("ledgerEntries").innerHTML=entries.map(e=>`<div class="list-item"><div><strong>${e.title}</strong><small>${e.date} • ${e.detail}</small></div><div class="money ${e.amount>0?"negative":"positive"}">${e.amount>0?"+":"-"}${money(Math.abs(e.amount))}</div></div>`).join("")||`<div class="list-item"><div><strong>ยังไม่มีประวัติเครดิต</strong></div></div>`}window.openLedger=id=>{selectedLedgerCustomerId=id;renderLedger();switchTab("ledger")}
function renderPayments(){$("paymentList").innerHTML=state.payments.slice(0,25).map(p=>`<div class="list-item"><div><strong>${customerName(p.customerId)}</strong><small>${p.date} • ${p.method} ${p.note?`• ${p.note}`:""}</small></div><div class="money positive">${money(p.amount)}</div></div>`).join("")||`<div class="list-item"><div><strong>ยังไม่มีประวัติรับเงิน</strong></div></div>`}
function filteredBills(){const from=$("reportFrom")?.value||"",to=$("reportTo")?.value||"",cid=$("reportCustomer")?.value||"",type=$("reportPaymentType")?.value||"";return state.bills.filter(b=>{if(b.status==="cancelled")return false;if(from&&b.date<from)return false;if(to&&b.date>to)return false;if(cid&&b.customerId!==cid)return false;if(type&&b.paymentType!==type)return false;return true})}function renderReports(){const rows=filteredBills();$("reportSales").textContent=money(rows.reduce((s,b)=>s+Number(b.subtotal||0),0));$("reportCost").textContent=money(rows.reduce((s,b)=>s+Number(b.costTotal||0),0));$("reportProfit").textContent=money(rows.reduce((s,b)=>s+Number(b.profitTotal||0),0));$("reportCount").textContent=rows.length.toLocaleString("th-TH");$("reportBills").innerHTML=rows.map(b=>`<tr><td>${b.billNo}</td><td>${b.date}</td><td>${customerName(b.customerId)}</td><td>${money(b.subtotal)}</td><td class="${b.profitTotal>=0?"positive":"negative"}">${money(b.profitTotal)}</td><td>${billBadge(b)}</td><td><button class="small-btn small-danger" onclick="cancelBill('${b.id}')">ยกเลิก</button></td></tr>`).join("")||`<tr><td colspan="7">ไม่พบรายการขาย</td></tr>`}
function renderBackupStatus(){const t=localStorage.getItem("khaikhongV2LastBackup");if($("backupStatus"))$("backupStatus").textContent=`Backup: ${t?new Date(t).toLocaleString("th-TH"):"ยังไม่เคย"}`}
function switchTab(id){document.querySelectorAll(".page").forEach(p=>p.classList.toggle("active",p.id===id));document.querySelectorAll(".tab").forEach(t=>t.classList.toggle("active",t.dataset.tab===id));window.scrollTo({top:0,behavior:"smooth"})}document.querySelectorAll(".tab,[data-open-tab]").forEach(el=>el.addEventListener("click",()=>switchTab(el.dataset.tab||el.dataset.openTab)));function setDates(){["billDate","purchaseDate","paymentDate"].forEach(id=>$(id).value=today())}
$("paymentType").addEventListener("change",()=>{$("customerField").classList.toggle("hidden-field",$("paymentType").value!=="credit")});$("saleSearch").addEventListener("input",renderSale);$("productSearch").addEventListener("input",renderProducts);$("customerSearch").addEventListener("input",renderCustomers);$("ledgerSearch").addEventListener("input",renderLedger);$("clearLedgerBtn").addEventListener("click",()=>{selectedLedgerCustomerId="";renderLedger()});$("clearCartBtn").addEventListener("click",clearCart);$("saveBillBtn").addEventListener("click",saveBill);
$("productForm").addEventListener("submit",async e=>{e.preventDefault();const id=$("productId").value||uid(),old=productById(id)||{};await put("products",{...old,id,name:$("productName").value.trim(),unit:$("productUnit").value.trim(),price:Number($("productPrice").value||0),minStock:Number($("productMin").value||0),note:$("productNote").value.trim(),stockQty:Number(old.stockQty||0),avgCost:Number(old.avgCost||0),createdAt:old.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()});resetProductForm();await loadState();showToast("บันทึกสินค้าแล้ว")});$("resetProductBtn").addEventListener("click",resetProductForm);
$("customerForm").addEventListener("submit",async e=>{e.preventDefault();const id=$("customerId").value||uid(),old=state.customers.find(c=>c.id===id)||{};await put("customers",{...old,id,name:$("customerName").value.trim(),type:$("customerType").value,phone:$("customerPhone").value.trim(),creditLimit:Number($("customerLimit").value||0),creditDays:Number($("customerDays").value||0),note:$("customerNote").value.trim(),createdAt:old.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()});resetCustomerForm();await loadState();showToast("บันทึกลูกค้าแล้ว")});$("resetCustomerBtn").addEventListener("click",resetCustomerForm);
$("purchaseForm").addEventListener("submit",async e=>{e.preventDefault();const productId=$("purchaseProduct").value;if(!productId)return alert("กรุณาเลือกสินค้า");const qty=Number($("purchaseQty").value||0),cost=Number($("purchaseCost").value||0);if(qty<=0)return alert("กรุณาใส่จำนวน");await put("stock_movements",{id:uid(),productId,type:"purchase",refType:"purchase",refId:"",date:$("purchaseDate").value||today(),qtyIn:qty,qtyOut:0,unitCost:cost,note:$("purchaseNote").value.trim(),createdAt:new Date().toISOString()});$("purchaseQty").value="";$("purchaseCost").value="";$("purchaseNote").value="";await recomputeInventory();await loadState();showToast("บันทึกซื้อเข้าแล้ว")});
$("paymentForm").addEventListener("submit",async e=>{e.preventDefault();const cid=$("paymentCustomer").value,amount=Number($("paymentAmount").value||0);if(!cid)return alert("กรุณาเลือกลูกค้า");if(amount<=0)return alert("กรุณาใส่จำนวนเงิน");await put("payments",{id:uid(),customerId:cid,billId:"",date:$("paymentDate").value||today(),amount,method:$("paymentMethod").value,note:$("paymentNote").value.trim(),createdAt:new Date().toISOString()});$("paymentAmount").value="";$("paymentNote").value="";await loadState();showToast("บันทึกรับเงินแล้ว")});
["reportFrom","reportTo","reportCustomer","reportPaymentType"].forEach(id=>$(id).addEventListener("input",renderReports));$("filterTodayBtn").addEventListener("click",()=>{$("reportFrom").value=today();$("reportTo").value=today();renderReports()});$("filterMonthBtn").addEventListener("click",()=>{const p=today().slice(0,7);$("reportFrom").value=`${p}-01`;const last=new Date(new Date().getFullYear(),new Date().getMonth()+1,0).getDate();$("reportTo").value=`${p}-${String(last).padStart(2,"0")}`;renderReports()});$("resetFilterBtn").addEventListener("click",()=>{["reportFrom","reportTo","reportCustomer","reportPaymentType"].forEach(id=>$(id).value="");renderReports()});
function download(filename,content,type="application/octet-stream"){const blob=new Blob([content],{type}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=filename;a.click();URL.revokeObjectURL(url)}$("exportCsvBtn").addEventListener("click",()=>{const rows=[["billNo","date","customer","subtotal","cost","profit","status"]];filteredBills().forEach(b=>rows.push([b.billNo,b.date,customerName(b.customerId),b.subtotal,b.costTotal,b.profitTotal,b.status]));const csv=rows.map(r=>r.map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(",")).join("\n");download(`khaikhong-v2-report-${today()}.csv`,"\ufeff"+csv,"text/csv;charset=utf-8")});$("exportBackupBtn").addEventListener("click",()=>{const data={app:"Khaikhong",version:"2.0.0",exportedAt:new Date().toISOString(),...state};localStorage.setItem("khaikhongV2LastBackup",new Date().toISOString());download(`khaikhong-v2-backup-${today()}.json`,JSON.stringify(data,null,2),"application/json");renderBackupStatus();showToast("สร้าง Backup แล้ว")});$("importBackupInput").addEventListener("change",async e=>{const file=e.target.files[0];if(!file)return;const data=JSON.parse(await file.text());if(!confirm("นำเข้า Backup จะเขียนทับข้อมูลในเครื่องนี้ ต้องการทำต่อไหม?"))return;for(const s of STORES)await clearStore(s);for(const s of STORES){for(const item of(data[s]||[]))await put(s,item)}await recomputeInventory();await recalcBills();await loadState();showToast("นำเข้า Backup แล้ว")});$("clearAllBtn").addEventListener("click",async()=>{if(!confirm("ล้างข้อมูลทั้งหมด? แนะนำให้ Backup ก่อน"))return;for(const s of STORES)await clearStore(s);cart=[];await loadState();showToast("ล้างข้อมูลแล้ว")});
function openNumberPad(input){currentNumberInput=input;numberPadValue=String(input.value||"");$("numpadTargetLabel").textContent=input.closest("label")?.childNodes[0]?.textContent?.trim()||"ใส่ตัวเลข";$("numpadDisplay").textContent=numberPadValue||"0";$("numberPadOverlay").classList.remove("hidden")}function closeNumberPad(){$("numberPadOverlay").classList.add("hidden");currentNumberInput=null;numberPadValue=""}function setPad(v){numberPadValue=v;$("numpadDisplay").textContent=numberPadValue||"0"}document.addEventListener("focusin",e=>{if(e.target?.matches?.('input[data-keypad="true"]')){e.target.blur();openNumberPad(e.target)}});document.querySelectorAll("[data-num]").forEach(btn=>btn.addEventListener("click",()=>{const n=btn.dataset.num;if(n==="."&&numberPadValue.includes("."))return;if(numberPadValue==="0"&&n!==".")setPad(n);else setPad(numberPadValue+n)}));$("numpadBack").addEventListener("click",()=>setPad(numberPadValue.slice(0,-1)));$("numpadClear").addEventListener("click",()=>setPad(""));$("numpadOk").addEventListener("click",()=>{if(currentNumberInput){currentNumberInput.value=numberPadValue||"0";currentNumberInput.dispatchEvent(new Event("input",{bubbles:true}));currentNumberInput.dispatchEvent(new Event("change",{bubbles:true}))}closeNumberPad()});$("numpadClose").addEventListener("click",closeNumberPad);$("numberPadOverlay").addEventListener("click",e=>{if(e.target.id==="numberPadOverlay")closeNumberPad()});
window.addEventListener("beforeinstallprompt",e=>{e.preventDefault();deferredPrompt=e;$("installBtn").classList.remove("hidden")});$("installBtn").addEventListener("click",async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;$("installBtn").classList.add("hidden")});if("serviceWorker"in navigator)window.addEventListener("load",()=>navigator.serviceWorker.register("./sw.js").catch(console.warn));
(async function init(){db=await openDB();setDates();await recomputeInventory();await recalcBills();await loadState()})();
